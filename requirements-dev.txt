# Development tooling. Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
ruff>=0.6
black>=24.0
pyinstaller>=6.0
httpx>=0.27        # FastAPI TestClient (for testing api.py)
